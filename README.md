 1.transform.position += new Vector3(x, y, z); = **For character movement**
 
 2.if (Input.GetKeyUp(KeyCode.W)){
       (condition)                          =**For keyboard control**//Change the W to any key you want
 }    
3.
 
